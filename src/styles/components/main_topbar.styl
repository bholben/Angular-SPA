@import '../vars'
@media (min-width:minWidthPhone)

// Top bar wrapper
  div.topbar
    fixed: left right
    height: topBarHeight
    z-index: zIndexTopBar
    color: cGrayE
    background-color: cBaseDark

    i.fa-bars, div.title, i.fa-user
      line-height: topBarHeight
      font-size: 30px
      cursor: pointer

    // Main menu toggle icon (hamburger icon)
    i.fa-bars
      float: left
      width: 100px
      padding-left: sidePadding

    // Title
    div.title
      width: 150px
      margin: 0 auto
      text-align: center
      font-size: 18px

    // User menu toggle icon (avatar icon)
    i.fa-user
      float:right
      width: 100px
      text-align: right

    // User menu wrapper
    nav.user
      fixed: top (topBarHeight - 5px) right
      width: 200px
      margin: 0 16px
      line-height: menuUserButtonHeight
      color: cGray5
      background-color: cGrayE
      ul.user-menu-items
        overflow: auto
        li
          height: menuUserButtonHeight
          padding-left: sidePadding
          border-bottom: 1px solid cGrayC
          font-size: 15px
          cursor: pointer
        li:hover
          background-color: cGrayC
      ul.user-menu-items:after
        absolute: top -8px right 15px
        border-bottom: 8px solid #eee
        border-right: 8px solid transparent
        border-left: 8px solid transparent
        content: ''

